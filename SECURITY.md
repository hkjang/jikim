# 보안 정책

jikim은 Secret, 암호화 키와 인증 토큰을 다루므로 일반 기능 버그와 보안 취약점을 구분하여 접수합니다.

## 지원 버전

프로젝트 초기 릴리스 동안 최신 `v0.1.x`만 보안 수정 대상입니다. 지원 범위가 바뀌면 이 표를 갱신합니다.

| 버전 | 보안 지원 |
| --- | --- |
| `v0.1.x` | 지원 |
| 미릴리스 main | 개발 검증용 |
| 이전 버전 | 미지원 |

## 비공개 신고

Secret 노출, 인증 우회, 권한 상승, 암호화 오류, SSRF, SQL injection, 원격 코드 실행 또는 공급망 문제는 공개 Issue를 만들지 마십시오.

1. 저장소의 **Security → Advisories → Report a vulnerability**를 사용합니다.
2. 해당 기능이 보이지 않으면 저장소 소유자에게 GitHub를 통해 비공개 연락을 먼저 요청합니다.
3. 응답을 받기 전에 exploit, 토큰, 덤프 또는 운영 정보를 공개하지 않습니다.

신고에 포함할 내용:

- 영향 버전과 commit SHA
- 공격 전제와 영향
- 최소 재현 단계 또는 테스트 코드
- 관련 endpoint와 역할
- 로그는 Secret·토큰·DSN을 제거한 뒤 첨부
- 제안 완화책이 있다면 함께 설명

실제 운영 Secret, 개인 데이터, 마스터 키, 세션 토큰은 어떤 채널로도 보내지 마십시오.

## 응답 목표

유지관리자는 가능한 범위에서 다음을 목표로 합니다.

- 영업일 3일 이내 접수 확인
- 영업일 10일 이내 재현 및 심각도 1차 판단
- 수정 가능 시 비공개 패치와 릴리스 계획 공유
- 릴리스 후 영향 범위, 완화 방법과 기여자 표기 협의

이는 서비스 수준 보장이 아니라 프로젝트 운영 목표입니다. 복잡도와 유지관리자 상황에 따라 달라질 수 있습니다.

## 안전한 연구 범위

- 본인이 소유한 로컬 테스트 환경만 사용
- 다른 사용자의 데이터에 접근하지 않음
- 서비스 거부, 대량 스캔, 사회공학 금지
- GitHub, Keycloak, PostgreSQL 또는 AI 제공자의 인프라를 허가 없이 시험하지 않음
- 취약성 확인에 필요한 최소 데이터만 사용하고 종료 후 안전하게 폐기

제3자 제품의 취약점은 해당 공급자에게 먼저 신고하십시오.

## 운영 중 의심 사고

1. 관련 토큰과 세션을 폐기합니다.
2. 필요하면 관리 경로를 격리하고 egress를 차단합니다.
3. PostgreSQL과 감사 로그의 보존 사본을 만듭니다.
4. 영향 Secret, 사용자 키, OIDC Client와 연결 시스템을 식별합니다.
5. 대상 시스템과 jikim 자격증명을 계획된 순서로 회전합니다.
6. 깨끗한 이미지와 검증된 백업으로 복구합니다.

자세한 운영 통제는 [`docs/guides/security.md`](docs/guides/security.md)를 참조하십시오.
